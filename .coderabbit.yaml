# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
language: en-US
tone_instructions: >-
  Be concise, specific, and constructive. Prioritize defects that can affect correctness,
  security, compatibility, operability, or maintainability. Avoid stylistic comments already
  enforced by repository tooling and do not repeat the same finding.
early_access: false
enable_free_tier: true

reviews:
  profile: chill
  request_changes_workflow: true
  high_level_summary: true
  high_level_summary_in_walkthrough: true
  high_level_summary_instructions: >-
    Summarize behavioral impact, compatibility risk, security implications, and validation.
  poem: false
  in_progress_fortune: false
  review_status: true
  review_progress: true
  fail_commit_status: true
  changed_files_summary: true
  collapse_walkthrough: false
  sequence_diagrams: false
  suggested_labels: false
  suggested_reviewers: false
  auto_apply_labels: false
  auto_assign_reviewers: false
  assess_linked_issues: false
  related_issues: false
  related_prs: false
  enable_prompt_for_ai_agents: false

  path_instructions:
    - path: "**/*"
      instructions: >-
        Review for correctness, security, backward compatibility, and sufficient focused tests.
        Flag undocumented user-visible behavior and dependency or workflow changes that weaken
        reproducibility, least privilege, or supply-chain safety. GitHub Actions must be pinned to
        immutable commit SHAs. Treat formatting-only preferences as non-blocking when automation
        already enforces them.
    - path: "sdks/core/src/**"
      instructions: >-
        Preserve the public TypeScript API and the POST /events wire contract. The SDK accepts
        publishable keys only and must never accept or expose Tinybird credentials. Verify
        organization, workspace, event, user, anonymous, and session identity semantics. Retries
        must preserve event_id and the original timestamp, avoid duplicate logical events, respect
        bounded backoff and batching, and remain safe during flush and shutdown. Require focused
        unit and integration tests for behavioral changes.
    - path: "sdks/browser/src/**"
      instructions: >-
        Review browser lifecycle, unload/pagehide delivery, auto-capture privacy, singleton state,
        and SSR or non-DOM safety. Preserve publishable-key-only authentication, bundle-size
        budgets, tree shaking, supported browser behavior, and the documented public API.
    - path: "sdks/browser-snippet/src/**"
      instructions: >-
        Preserve pre-load queue ordering, loader idempotence, initialization timing, CSP-friendly
        behavior, configuration validation, and a small browser footprint. Never expose secrets or
        route browser traffic to tenant-scoped analytics read APIs.
    - path: "specs/event-schema.json"
      instructions: >-
        Treat this as a cross-SDK and worker compatibility boundary. Block uncoordinated breaking
        changes, relaxed identity requirements, mutable timestamp semantics, or schema drift that
        is not covered by conformance tests and migration notes.
    - path: "sdks/**/tests/**"
      instructions: >-
        Keep tests deterministic and offline. Cover retries, duplicate prevention, timestamp and
        identity preservation, lifecycle cleanup, public API compatibility, and error paths
        affected by the change.
    - path: "**/package.json"
      instructions: >-
        Check dependency necessity, exact public entry points, side-effects and tree-shaking
        behavior, supported runtimes, bundle impact, and release-script safety. Treat lockfile
        changes as supply-chain-sensitive and ensure they match the manifest change.
    - path: ".coderabbit.yaml"
      instructions: >-
        Block changes that disable automatic review, request-changes behavior, exact-head gating,
        or the ignored-bot policy. Require live-schema validation for every configuration change.
    - path: ".github/workflows/coderabbit-gate.yml"
      instructions: >-
        This is a protected trust boundary. It must run from pull_request_target without checking
        out or executing pull-request code, use only the main-restricted coderabbit-gate
        environment, and publish through the dedicated Alitycs Gate app. Ordinary pull requests
        must not alter this file.
    - path: ".github/workflows/**"
      instructions: >-
        Keep permissions least-privileged, actions pinned to full immutable commit SHAs, CI
        deterministic, and releases gated on the complete verification and package-integrity suite.

  auto_review:
    enabled: true
    auto_incremental_review: true
    auto_pause_after_reviewed_commits: 0
    drafts: false
    ignore_usernames:
      - "dependabot[bot]"
      - "renovate[bot]"
      - "github-actions[bot]"

  pre_merge_checks:
    override_requested_reviewers_only: true
    docstrings:
      mode: off
    title:
      mode: warning
      requirements: Use a concise, imperative title that describes the behavior changed.
    description:
      mode: warning
    issue_assessment:
      mode: warning

  tools:
    github-checks:
      enabled: true

  finishing_touches:
    docstrings:
      enabled: false
    unit_tests:
      enabled: false
    simplify:
      enabled: false
    autofix:
      enabled: false
    fix_ci:
      enabled: false
    resolve_merge_conflict:
      enabled: false
    custom: []

  post_merge_actions: []

chat:
  allow_non_org_members: false
  art: false
  auto_reply: true

knowledge_base:
  opt_out: true
  web_search:
    enabled: false
  automatic_repository_linking: false
  linked_repositories: []

issue_enrichment:
  auto_enrich:
    enabled: false
  planning:
    enabled: false
    auto_planning:
      enabled: false
      labels: []
  labeling:
    labeling_instructions: []
    auto_apply_labels: false
